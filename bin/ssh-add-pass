#!/bin/sh
ssh_cmd=("ssh-add")
if [ -x /usr/bin/keychain ]; then
  /usr/bin/keychain --quiet --clear "$1"
else
expect << EOF
  spawn "${ssh_cmd[@]}" "$1"
  expect "Enter passphrase"
  send "$2\n"
  expect eof
EOF
