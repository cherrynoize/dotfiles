#!/bin/bash
# rsync wrapper to prevent accidental trailing slashes
new_args=()
for i in "${@}"; do
  case "${i}" in
    /)
    ;;
    */)
      echo "ambiguous trailing slash: did you mean '${i%/}' or '${i}.'?"
      exit 1
    ;;
    esac
done
exec rsync "${@}"
